var src = 'src';
var app = src + '/app';
var assets = src + '/assets';

var build = {
    root: 'build',
    app: 'app'
};

var config = {
    src: src,
    app: app,
    assets: assets,
    build: build
};

module.exports = config;
